import { Event } from '@/constants/types';

export default function DialogBox({
    name, description, coverImage
}: Event) {
    return (
        <div>
            <div><h1></h1></div>
        </div>
    )
}